$(".right-conversation").html("<%= j render 'conversations/right', conversation: @conversation %>")
$(".chat").slimScroll({height: "85%"});
$(".chat").slimScroll({scrollTo: $(".chat")[0].scrollHeight});
$(".loading-spin").hide();
