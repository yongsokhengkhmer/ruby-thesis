$(".suggest-list").append("<%= j render partial: 'recommendations/suggest_list', collection: @users, as: :user %>")
$(".recommend-spinner").hide();
<% unless @result_ids.next_page %>
  $(".recommend-paginate .btn-load-more").remove();
<% end %>
